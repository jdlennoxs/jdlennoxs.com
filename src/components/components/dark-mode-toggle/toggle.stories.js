import { DarkModeToggle } from '.'

export default {
  title: 'Components/DarkModeToggle',
  component: DarkModeToggle,
}

const Template = (args) => <DarkModeToggle />

export const Default = Template.bind({})
